{
  "name": "fadr-808-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FADR-808 MCP server — Cloudflare Worker (Streamable HTTP) + local stdio.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "stdio": "node server.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
